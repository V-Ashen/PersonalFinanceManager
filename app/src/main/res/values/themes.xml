<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PersonalFinanceManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PersonalFinanceManager" parent="Base.Theme.PersonalFinanceManager" />

    <!-- Splash Screen Theme -->
    <!-- In res/values/themes.xml -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>

        <!-- CHANGE THIS LINE -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_centered</item>

        <item name="postSplashScreenTheme">@style/Theme.PersonalFinanceManager</item>
    </style>

</resources>